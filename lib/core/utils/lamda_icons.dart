import 'package:flutter/material.dart';
import 'lamda_images.dart';

const double size = 34.0;

const ImageIcon menuIcon = const ImageIcon(menuPng, size: size);
const ImageIcon filterIcon = const ImageIcon(filterPng, size: size);

const ImageIcon homeFilled = const ImageIcon(homePng, size: size);
const Icon favoriteFilled = const Icon(Icons.favorite, size: size);
const Icon userFilled = const Icon(Icons.person, size: size);
const Icon cartFilled = const Icon(Icons.shopping_cart, size: size);

const Icon homeOutline = const Icon(Icons.home_outlined, size: size);
const Icon favoriteOutline = const Icon(Icons.favorite_outline_rounded, size: size);
const Icon userOutline = const Icon(Icons.person_outline_rounded, size: size);
const Icon cartOutline = const Icon(Icons.shopping_cart_outlined, size: size);
